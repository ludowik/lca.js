class SketchPython extends Sketch {
    constructor(sketchName) {
        super();
        this.sketchName = sketchName;
    }

    setup(config) {
    }

    init() {
        //window.initGo();
    }

    update(dt) {
    }

    render() {
    }
}
